export default function Toast() {
  // Using react-hot-toast — this component is a placeholder for custom toast styling
  // Toasts are rendered via the <Toaster /> component in App.jsx
  return null;
}
